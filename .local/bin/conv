#!/usr/bin/env bash

sourceExt=$1
destExt=$2

for file in *.$sourceExt; do
    # ffmpeg -i "$file" -q:a 0 -map a "${file%.mp4}.mp3"
    ffmpeg -i "$file" -q:a 0 -map a "${file%.$sourceExt}.$destExt"
    rm "$file"
done
